{
  "name": "gtfs-data-fetching",
  "version": "1.0.0",
  "description": "Fetches data from GTFS feeds and stores it in Dynamo",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "release": "npm i && npm run build && rm -R node_modules && npm i --only prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pelayo Sanchez Margareto",
  "license": "MIT",
  "devDependencies": {
    "@middy/core": "^3.4.0",
    "@types/aws-lambda": "^8.10.106",
    "@types/node": "^18.7.18",
    "@types/unzip-stream": "^0.3.1",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1222.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^1.2.1",
    "fast-csv": "^4.3.6",
    "unzip-stream": "^0.3.1"
  }
}
